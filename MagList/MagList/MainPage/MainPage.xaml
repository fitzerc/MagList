<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MagList.MainPage"
             x:Class="MagList.MainPage.MainPage">

    <ScrollView>
        <VerticalStackLayout>

            <Grid Margin="3, 5, 5, 5" Padding="3, 5, 5, 5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="9*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Border StrokeShape="RoundRectangle 10,10,10,10"
                        Margin="5, 0, 5, 0"
                        StrokeThickness="1"
                        Stroke="{StaticResource Primary}"
                        BackgroundColor="{StaticResource Secondary}">
                    <Entry
                        BackgroundColor="{StaticResource Secondary}"
                        Grid.Column="0"
                        Margin="5, 0, 0, 0"
                        Placeholder="New Entry Name"
                        PlaceholderColor="{StaticResource Gray500}"
                        TextColor="{StaticResource Primary}"
                        Text="{Binding NewEntryName}" />
                </Border>

                <Button BackgroundColor="{StaticResource Secondary}"
                    BorderColor="{StaticResource Primary}"
                    BorderWidth=".5"
                    Command="{Binding AddClickedCommand}"
                    Margin="0, 2, 0, 2" Grid.Column="1"
                    Text="+"
                    TextColor="{StaticResource Primary}"/>
            </Grid>

            <CollectionView ItemsSource="{Binding EntryList}" Margin="10, 0, 8, 0">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 10,10,10,10"
                            StrokeThickness="1"
                            Stroke="{StaticResource Primary}"
                            BackgroundColor="{StaticResource Secondary}">
                            <Border.GestureRecognizers>
                                <DragGestureRecognizer CanDrag="True" />
                            </Border.GestureRecognizers>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="87*" />
                                <ColumnDefinition Width="13*" />
                            </Grid.ColumnDefinitions>
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Name}" TextColor="{StaticResource Tertiary}" FontSize="Medium" Margin="5, 0, 0, 0" />
                                <Label Margin="15, 0, 0, 8" FontSize="Small" TextColor="{StaticResource Primary}" Text="{Binding Description}" />
                            </VerticalStackLayout>
                            <Button BackgroundColor="{StaticResource Secondary}"
                                VerticalOptions="Center"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPageViewModel}}, Path=DeleteClickedCommand}"
                                CommandParameter="{Binding .}"
                                Grid.Column="1"
                                Text="x"
                                TextColor="{StaticResource Primary}" />
                        </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
